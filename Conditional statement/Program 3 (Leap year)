#include <stdio.h>
int main()
{
    int Y;
    printf("Enter any Year:");
    scanf("%d",&Y);
    if((Y%4==0)&&(Y%100!=0)||(Y%400==0))
    {
        printf("These is a Leap year");
    }
    else
    {
        printf("These is not a Leap year");
    }
    return 0;
}
